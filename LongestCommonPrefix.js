var longestCommonPrefix = function(strs) {

    for (let i = 0; i < strs.length; i++){
        let word = strs[i]
        for (let j = 0; j<= word.length; j++){
            let letters = word.slice(0, 2)
        }
    }


    let commonArray = []
    let result = commonArray.join('')
    return result
    
};